spring:
  application:
    name: cinema-microservice

  cloud:
    config:
      uri: http://localhost:8888
      username: admin
      password: config

logging:
  level:
    org.springframework.web: error
