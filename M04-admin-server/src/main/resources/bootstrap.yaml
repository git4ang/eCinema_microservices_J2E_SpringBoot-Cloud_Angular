spring:
  application:
    name: admin-server
  cloud:
    config:
      uri: http://localhost:8888
      username: admin
      password: config
  boot:
    admin:
      discovery:
        ignored-services: ${spring.application.name}
