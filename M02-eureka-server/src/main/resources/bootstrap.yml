spring:
  application:
    name: eureka-server

  cloud:
    config:
      uri: http://localhost:8888
      username: admin
      password: config
  profiles:
    active: dev, test, native

logging:
  level:
    org.springframework.cloud: DEBUG